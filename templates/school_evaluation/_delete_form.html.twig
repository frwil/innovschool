<form style="display: inline;" method="post" action="{{ path('app_school_evaluation_delete', {'id': school_evaluation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ school_evaluation.id) }}">
    <button class="btn btn-danger btn-lg">Supprimer</button>
</form>
