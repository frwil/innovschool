<form method="post" action="{{ path('app_school_class_subject_group_delete', {'id': school_class_subject_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ school_class_subject_group.id) }}">
    <button class="btn">Delete</button>
</form>
